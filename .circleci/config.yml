version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: Install dependencies and build project
          command: "echo Hello, World!"

workflows:
  main:
    jobs:
      - build